#!/bin/bash

if [ $# -ge 1 ]
then x=$1
else x=1
fi
if [ $# -ge 2 ]
then baker=$2
else baker="bootstrap1"
fi

echo "bake $x time with $baker"

n=0
while (( $n < $x ))
do
    tezos-client bake for $baker --minimal-timestamp
    n=$(( n + 1 ))
done
